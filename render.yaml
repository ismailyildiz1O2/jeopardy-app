services:
  - type: web
    name: jeopardy-backend
    runtime: node
    region: ohio
    buildCommand: npm install && cd packages/backend && npm run build
    startCommand: cd packages/backend && node dist/index.js
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: FRONTEND_URL
        sync: false
      - key: NODE_ENV
        value: production
